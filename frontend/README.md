Front-end prototype for TRACE UI

Install and run:

```
cd frontend
npm install
npm run dev
```

This is a minimal Vite + React prototype containing the sidebar, agent cards, upload area and chat input.
